{
  "name": "bihe-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "BIHE College website monorepo — delegates to Frontend/ and Backend/",
  "scripts": {
    "dev": "npm run dev --prefix Frontend",
    "dev:clean": "npm run dev:clean --prefix Frontend",
    "dev:fast": "npm run dev:fast --prefix Frontend",
    "dev:status": "npm run dev:status --prefix Frontend",
    "clean": "npm run clean --prefix Frontend",
    "verify": "npm run verify --prefix Frontend",
    "verify:backend": "npm run verify:backend --prefix Frontend",
    "api:check": "npm run api:check --prefix Frontend",
    "build": "npm run build --prefix Frontend",
    "build:local": "npm run build:local --prefix Frontend",
    "build:hostinger": "npm run build:hostinger --prefix Frontend",
    "start": "npm run start --prefix Frontend",
    "start:local": "npm run start:local --prefix Frontend",
    "start:hostinger": "npm run start:hostinger --prefix Frontend",
    "lint": "npm run lint --prefix Frontend",
    "download-assets": "npm run download-assets --prefix Frontend",
    "import:dropbox-gallery": "npm run import:dropbox-gallery --prefix Frontend",
    "export:site-pages": "npm run export:site-pages --prefix Frontend",
    "package:deploy": "bash scripts/package-all-for-deploy.sh"
  }
}
